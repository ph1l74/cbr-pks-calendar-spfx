import * as React from 'react';
import styles from './RcrCalendar.module.scss';
import { IEventCardProps } from './IEvenCardProps';

export default class RcrCalendar extends React.Component<IEventCardProps, {}> {
  public render(): React.ReactElement<IEventCardProps> {
    return (
      <div className="card">

      </div>
    );
  }
}
